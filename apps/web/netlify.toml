[build]
  base = "apps/web"
  command = "cd ../.. && corepack enable && pnpm install --frozen-lockfile && pnpm build:web"
  publish = ".next"

[build.environment]
  NODE_VERSION = "22"

[[plugins]]
  package = "@netlify/plugin-nextjs"
